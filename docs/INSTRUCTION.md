# คู่มือการใช้งานระบบ Digitize ข้อมูลบัญชีทรัพย์สิน ป.ป.ช.

## 📚 สารบัญ

1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การติดตั้ง](#การติดตั้ง)
3. [การตั้งค่า API Key](#การตั้งค่า-api-key)
4. [วิธีการใช้งาน](#วิธีการใช้งาน)
5. [ผลลัพธ์](#ผลลัพธ์)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## ภาพรวมระบบ

ระบบนี้ใช้ **Google Gemini 2.0 Flash AI** ในการอ่านและแปลงเอกสาร PDF บัญชีทรัพย์สินและหนี้สินของ ป.ป.ช. ให้อยู่ในรูปแบบดิจิทัล (CSV) ที่มีความแม่นยำและมีมาตรฐาน

### ความสามารถหลัก

- ✅ อ่าน PDF ภาษาไทยได้อัตโนมัติ
- ✅ แปลงข้อมูลเป็น 13 ไฟล์ CSV ตามโครงสร้าง Database
- ✅ รองรับทั้งชุดข้อมูล Training และ Test
- ✅ จัดการข้อมูลวันที่ ตัวเลข และข้อความภาษาไทย

---

## การติดตั้ง

### ขั้นตอนที่ 1: ตรวจสอบ Python

ต้องมี Python 3.8 ขึ้นไป

```bash
python --version
# หรือ
python3 --version
```

### ขั้นตอนที่ 2: สร้าง Virtual Environment (แนะนำ)

```bash
# สร้าง virtual environment
python -m venv .venv

# เปิดใช้งาน
# macOS/Linux:
source .venv/bin/activate

# Windows:
.venv\\Scripts\\activate
```

### ขั้นตอนที่ 3: ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

รายการ Libraries ที่ใช้:
- `google-generativeai` - สำหรับ Gemini AI API
- `pandas` - จัดการและสร้างไฟล์ CSV
- `python-dateutil` - จัดการวันที่
- `tqdm` - แสดง Progress Bar
- `PyPDF2` - สำรองสำหรับอ่าน PDF
- `python-dotenv` - จัดการ Environment Variables

---

## การตั้งค่า API Key

### ขั้นตอนที่ 1: ขอ API Key ฟรีจาก Google

1. ไปที่ https://aistudio.google.com/apikey
2. ล็อกอินด้วย Google Account
3. คลิก "Create API Key"
4. คัดลอก API Key ที่ได้

### ขั้นตอนที่ 2: ตั้งค่า API Key

**วิธีที่ 1: ใช้ไฟล์ .env (แนะนำ)**

```bash
# คัดลอกไฟล์ตัวอย่าง
cp .env.example .env

# แก้ไขไฟล์ .env ใส่ API Key ของคุณ
nano .env
```

ใน `.env`:
```
GEMINI_API_KEY=your_actual_api_key_here
```

**วิธีที่ 2: ตั้งค่า Environment Variable**

```bash
# macOS/Linux
export GEMINI_API_KEY="your_api_key_here"

# Windows (Command Prompt)
set GEMINI_API_KEY=your_api_key_here

# Windows (PowerShell)
$env:GEMINI_API_KEY="your_api_key_here"
```

**วิธีที่ 3: ใส่ในคำสั่งโดยตรง**

```bash
python main.py --api-key your_api_key_here --mode train
```

---

## วิธีการใช้งาน

### โหมดที่ 1: ประมวลผลข้อมูล Training

ใช้สำหรับทดสอบระบบกับข้อมูล Training ที่มี Ground Truth

```bash
# ประมวลผลข้อมูล Training ทั้งหมด
python main.py --mode train

# ประมวลผลเฉพาะ 5 เอกสารแรก (สำหรับทดสอบ)
python main.py --mode train --limit 5
```

**ผลลัพธ์:**
- สร้างโฟลเดอร์ `output/train/`
- ไฟล์ CSV 13 ไฟล์: `Train_*.csv`

### โหมดที่ 2: ประมวลผลข้อมูล Test (สำหรับส่งแข่งขัน)

```bash
# ประมวลผลข้อมูล Test ทั้งหมด (23 เอกสาร)
python main.py --mode test
```

**ผลลัพธ์:**
- สร้างโฟลเดอร์ `output/test/`
- ไฟล์ CSV 13 ไฟล์: `Test_*.csv`
- **นำไฟล์เหล่านี้ส่งแข่งขันใน Kaggle และ Email**

### โหมดที่ 3: ประมวลผล PDF เดี่ยว

```bash
# ประมวลผล PDF แค่ 1 ไฟล์
python main.py --pdf "path/to/document.pdf"
```

---

## ผลลัพธ์

### ไฟล์ CSV ที่ได้ (13 ไฟล์)

1. **submitter_old_name.csv** - ข้อมูลการเปลี่ยนชื่อของผู้ยื่นเอกสาร
2. **submitter_position.csv** - ข้อมูลตำแหน่งงานของผู้ยื่นเอกสาร
3. **spouse_info.csv** - ข้อมูลคู่สมรสผู้ยื่นเอกสาร
4. **spouse_old_name.csv** - ข้อมูลการเปลี่ยนชื่อของคู่สมรส
5. **spouse_position.csv** - ข้อมูลตำแหน่งงานของคู่สมรส
6. **relative_info.csv** - ข้อมูลญาติผู้ยื่นเอกสาร
7. **statement.csv** - ข้อมูลรายการในระบบ
8. **statement_detail.csv** - ข้อมูลรายละเอียดรายการรอง
9. **asset.csv** - ข้อมูลทรัพย์สินทั้งหมด
10. **asset_building_info.csv** - ข้อมูลทรัพย์สินประเภทสิ่งปลูกสร้าง
11. **asset_land_info.csv** - ข้อมูลทรัพย์สินประเภทที่ดิน
12. **asset_vehicle_info.csv** - ข้อมูลทรัพย์สินประเภทยานพาหนะ
13. **asset_other_asset_info.csv** - ข้อมูลทรัพย์สินประเภทอื่น ๆ

### โครงสร้างโฟลเดอร์ผลลัพธ์

```
output/
├── train/              # ผลลัพธ์จาก Training data
│   ├── Train_submitter_old_name.csv
│   ├── Train_submitter_position.csv
│   ├── Train_spouse_info.csv
│   └── ... (อีก 10 ไฟล์)
│
└── test/               # ผลลัพธ์จาก Test data (สำหรับส่งแข่งขัน)
    ├── Test_submitter_old_name.csv
    ├── Test_submitter_position.csv
    ├── Test_spouse_info.csv
    └── ... (อีก 10 ไฟล์)
```

---

## การแก้ไขปัญหา

### ปัญหา: API Key Error

```
❌ Error: GEMINI_API_KEY not found!
```

**แก้ไข:**
- ตรวจสอบว่าตั้งค่า API Key ถูกต้องแล้ว
- ลองวิธีอื่นในการตั้งค่า API Key (ดูหัวข้อ "การตั้งค่า API Key")

### ปัญหา: PDF ไม่พบ

```
⚠️ PDF not found: filename.pdf
```

**แก้ไข:**
- ตรวจสอบว่าคัดลอกข้อมูลจาก Kaggle ครบ
- ตรวจสอบโครงสร้างโฟลเดอร์:
```
data/
├── training/
│   └── train input/
│       └── Train_pdf/
│           └── pdf/  <-- PDF ต้องอยู่ที่นี่
└── test final/
    └── test final input/
        └── Test final_pdf/
            └── pdf/  <-- PDF ต้องอยู่ที่นี่
```

### ปัญหา: เกิด Error ขณะประมวลผล

```
❌ Error processing filename.pdf: ...
```

**แก้ไข:**
- ระบบจะข้ามไฟล์ที่มีปัญหาและดำเนินการต่อ
- ดูรายละเอียด Error ใน Console
- ตรวจสอบไฟล์ PDF ว่าเปิดได้ หรือไม่เสียหาย

### ปัญหา: API Rate Limit

```
429 Resource has been exhausted
```

**แก้ไข:**
- รอสักครู่แล้วลองใหม่
- ใช้ `--limit` เพื่อประมวลผลทีละน้อย
```bash
python main.py --mode test --limit 5
```

### ปัญหา: ข้อมูลที่ได้ไม่ครบ

**แก้ไข:**
- ตรวจสอบว่า PDF มีข้อมูลครบถ้วน
- บางเอกสารอาจไม่มีข้อมูลบางส่วน (เช่น ไม่มีคู่สมรส)
- ระบบจะสร้างไฟล์ CSV ว่าง ๆ ถ้าไม่มีข้อมูล

---

## ข้อมูลเพิ่มเติม

### ขีดจำกัด Gemini API (Free Tier)

- **15 requests/minute**
- **1 million tokens/day**
- เพียงพอสำหรับข้อมูล Test 23 เอกสาร

### เวลาในการประมวลผล

- **1 PDF**: ~10-30 วินาที
- **23 PDFs (Test)**: ~10-15 นาที
- **69 PDFs (Training)**: ~30-45 นาที

### การปรับแต่ง

สามารถแก้ไขไฟล์ `src/config.py` เพื่อปรับแต่ง:
- `TEMPERATURE` - ความสุ่มของ AI (ต่ำ = สม่ำเสมอ)
- `MAX_RETRIES` - จำนวนครั้งที่ลองใหม่เมื่อเกิดข้อผิดพลาด

---

## 📧 การติดต่อ

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Error message ใน Console
2. ดู Logs ในโฟลเดอร์ output
3. ตรวจสอบเอกสาร Kaggle Competition

---

**สร้างโดย:** Hackathon Team  
**เวอร์ชัน:** 1.0  
**วันที่:** 6 ธันวาคม 2568  
**AI Model:** Google Gemini 2.0 Flash
